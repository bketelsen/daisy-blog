---
import Date from "@components/utilities/Date";
import { Picture } from "astro-imagetools/components";
const post = Astro.props.post
const {data: headerImage} = post.attributes.headerImage
console.log(headerImage.attributes.formats)


---

<div class="w-full px-4 pb-4 shadow-xl md:w-1/2 lg:w-1/3">
  <a class="block mb-6 overflow-hidden rounded-md" href={`/${post.attributes.slug}`}>

  </a>
  <p class="mb-2 font-medium text-blue-700">Brian •
    <Date date={post.attributes.date} />
  </p>
  <a class="inline-block mb-4 text-2xl font-bold leading-tight font-title text-coolGray-800 hover:text-coolGray-900 hover:underline"
    href={`/${post.attributes.slug}`}>{post.attributes.title}</a>
  <p class="mb-4 text-base font-medium md:text-lg text-coolGray-400">{post.attributes.summary}</p>

</div>
